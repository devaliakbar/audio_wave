name: audio_wave
description: A new Flutter application.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.14.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.4
  flutter_audio_recorder:
    git:
      url: https://github.com/devaliakbar/flutter_audio_recorder.git
  path_provider: ^2.0.9
  file: ^6.1.2
  just_audio: ^0.9.20

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/
